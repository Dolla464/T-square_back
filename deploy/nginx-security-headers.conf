# Example security headers for aaPanel / nginx (frontend + API)
#
# add_header X-Frame-Options "SAMEORIGIN" always;
# add_header X-Content-Type-Options "nosniff" always;
# add_header Referrer-Policy "strict-origin-when-cross-origin" always;
# add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
# Frontend SPA (tsquarecenter.com) — allow API + Reverb WebSocket:
# add_header Content-Security-Policy "default-src 'self'; script-src 'self'; style-src 'self' 'unsafe-inline'; img-src 'self' data: blob: https:; media-src 'self' blob: https://api.tsquarecenter.com; connect-src 'self' https: https://api.tsquarecenter.com wss://api.tsquarecenter.com wss://api.tsquarecenter.com:8080 ws: wss:; font-src 'self' data:; frame-src 'self' blob:; object-src 'none'; base-uri 'self'; form-action 'self';" always;
